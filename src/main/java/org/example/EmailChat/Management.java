package org.example.EmailChat;

import java.util.HashMap;
import java.util.Map;

public class Management {
    public static final Map<Long,Step> USER_STEPS=new HashMap<>();
    public static final Map<Long,Information> USER_MASSAGES=new HashMap<>();
    public static final Map<Long,SenderInformation> USER_SENDER=new HashMap<>();
}
